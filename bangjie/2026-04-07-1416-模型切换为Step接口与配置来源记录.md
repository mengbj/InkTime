# 模型切换为Step接口与配置来源记录

记录时间：2026-04-07 14:16（Asia/Shanghai）

## 本轮变更

用户要求：

- 不再沿用前面提到的千问 / Qwen 模型配置
- 改为以 `/Users/apple/bangjie/dev_jy/InkTime/bangjie/step.md` 作为当前模型配置来源
- 并将这一点记录到文档中

---

## 当前确认到的模型来源文件

配置来源文件：

- `/Users/apple/bangjie/dev_jy/InkTime/bangjie/step.md`

文件中记录的是一组 Step 平台配置，核心含义为：

- 模型改为 `step-3.5-flash`
- 接口基址改为 `https://api.stepfun.com/v1`
- 认证方式为 Step 平台对应的 API Key

因此，从这一步开始，前面文档中如果出现：

- `qwen3-vl-32b-instruct`
- `Qwen2.5-VL-7B-Instruct`
- 本地 OpenAI 兼容视觉模型仅作为建议

都应理解为：

- **当前实际优先使用 `step.md` 中定义的 Step 模型配置进行替换**

---

## 对当前项目的实际意义

这次模型切换主要影响的是“图片理解 / 内容生成 / 每日照片分析”这条链路，典型包括：

- `/Users/apple/bangjie/dev_jy/InkTime/analyze_photos.py`
- `/Users/apple/bangjie/dev_jy/InkTime/render_daily_photo.py`
- `/Users/apple/bangjie/dev_jy/InkTime/server.py`
- `/Users/apple/bangjie/dev_jy/InkTime/config.py`

也就是说：

- ESP32 烧录
- 首次配网
- HTTP 下载 `.bin`
- 墨水屏 SPI 刷新

这些硬件链路与 Step/Qwen 模型本身无关；
模型只影响“服务器侧如何生成要下发给屏幕的内容”。

---

## 与当前调试阶段的关系

截至本轮，硬件联调主线仍然是：

- ESP32 连接 `mengbj_iPhone`
- Mac 使用热点 IP `172.20.10.9`
- 服务监听 `8765`
- 下载测试 bin 文件
- 验证墨水屏是否真正刷新

因此当前阶段可以分成两层：

### 第一层：先点亮屏幕

先用已经生成好的测试 bin：

- `photo_0.bin`
- `photo_1.bin`
- `photo_2.bin`
- `photo_3.bin`
- `photo_4.bin`

先把“下载 + 刷屏”链路跑通。

### 第二层：再接入 Step 模型生成正式内容

等屏幕点亮后，再把服务端的模型配置切换到：

- `step-3.5-flash`
- `https://api.stepfun.com/v1`

用于真实图片理解与内容生成。

---

## 当前建议

当前最稳的推进顺序仍然是：

1. 先保持 `172.20.10.9:8765` 跑通测试 bin 下载
2. 先确认 ESP32 串口进入 `HTTP code=200`
3. 先确认墨水屏确实能刷新测试图案
4. 再回头把服务端模型配置正式替换为 `step.md` 对应配置

这样可以避免把“模型问题”和“硬件问题”混在一起排查。

---

## 安全补充

`/Users/apple/bangjie/dev_jy/InkTime/bangjie/step.md` 中包含敏感认证信息，后续建议：

- 不要把该文件直接提交到公开仓库
- 更稳妥的做法是改成环境变量或本地私有配置文件

---

## 本轮一句话记录

后续涉及视觉理解/内容生成的模型配置，以 `/Users/apple/bangjie/dev_jy/InkTime/bangjie/step.md` 为准，
优先使用 Step 平台的 `step-3.5-flash`，但当前硬件联调仍先以测试 bin 点亮墨水屏为第一目标。

