# 配网页已打开与后端服务准备状态

记录时间：2026-04-07 13:00（Asia/Shanghai）

## 本轮结论

这一步已经取得两个关键进展：

- ESP32 配网页已经成功打开；
- 本机 InkTime 后端依赖已安装，并已成功启动 `server.py`。

因此当前项目已经从“烧录与串口联通”推进到：
- `ESP32 配网页可访问` -> `准备填写家里/办公室 Wi‑Fi 与服务器地址` -> `再推进到实际拉取 BIN 并刷新墨水屏`。

## 截图里已经确认到的状态

用户本轮截图清楚显示：

### 1. 配网页已经真的打开了
浏览器页面已显示：
- `InkTime 设置`
- `WiFi SSID`
- `密码`
- `服务器 (host:port)`
- `每日刷新时间`
- `时区`
- `画面旋转 180°`
- `保存并重启`

说明：
- `http://192.168.4.1/` 已经能通；
- AP 模式和 WebServer 都是正常的；
- 之前的超时问题已经解决。

### 2. 串口中已经出现 HTTP 访问记录
截图串口中可见：
- `[HTTP] GET /`
- `[CFG] scanNetworks n=13`

说明：
- 浏览器访问根路径时，ESP32 已经收到了请求；
- 配网页不是假打开，而是实际由板子上的 `WebServer` 响应。

## 代码与服务端检查结果

### 1. ESP32 端的实际下载路径规则
本轮检查主工程：
- `/Users/apple/bangjie/dev_jy/InkTime/esp32/ink-display-7C-photo/ink-display-7C-photo.ino`

确认到：
- 当前固件会从配置里的 `host:port` 拼出下载地址；
- 固定路径前缀是：
  - `/static/inktime/yourdownloadkey/photo_`
- 随机选 `0..4` 共 5 张之一；
- 也就是最终会请求类似：
  - `http://<host:port>/static/inktime/yourdownloadkey/photo_0.bin`
  - `http://<host:port>/static/inktime/yourdownloadkey/photo_1.bin`
  - ...
  - `http://<host:port>/static/inktime/yourdownloadkey/photo_4.bin`

### 2. 本机 Python 环境已补齐
本轮已在仓库下创建：
- `/Users/apple/bangjie/dev_jy/InkTime/.venv`

并已安装 `requirements.txt` 中依赖：
- `Flask==3.1.2`
- `requests==2.32.5`
- `Pillow==12.0.0`

### 3. 本机 `config.py` 已补上
由于原仓库只有示例配置：
- `config-example.py`

本轮已复制生成：
- `/Users/apple/bangjie/dev_jy/InkTime/config.py`

当前是示例默认值，后续仍需按用户自己的照片目录/模型服务做进一步调整。

### 4. 本机 `server.py` 已成功启动
本轮已用虚拟环境启动：
- `/Users/apple/bangjie/dev_jy/InkTime/server.py`

服务启动日志确认：
- `listen: 0.0.0.0:8765`
- 本机可打开：`http://127.0.0.1:8765/`
- 局域网可访问：`http://10.142.16.119:8765/`

并且已验证：
- `http://127.0.0.1:8765/` 返回 `302` 跳转到 `/review`

说明：
- Flask 服务已经起来了；
- `host:port` 可以填写 `10.142.16.119:8765`，**前提是稍后 ESP32 连接到的 Wi‑Fi 与这台 Mac 所在局域网确实互通**。

## 当前仍未完成的关键点

### 1. 墨水屏实际下载文件还不存在
本轮验证：
- `http://127.0.0.1:8765/static/inktime/yourdownloadkey/photo_0.bin`
当前返回：
- `404 NOT FOUND`

说明：
- 服务端已经起来；
- 但真正给 7.3 寸四色屏下载的 BIN 文件还没有准备好；
- 所以即便现在把 Wi‑Fi 和服务器地址保存给 ESP32，后续联网下载时仍可能因为文件不存在而失败。

### 2. 也就是说，当前离“真正点亮项目内容”还差渲染产物
还需要补齐：
- `photo_0.bin` 到 `photo_4.bin`
- 对应输出目录与静态映射
- 如需自动从照片生成这些内容，则还要准备视觉模型接口与照片目录

## 现在该怎么继续

### 路线 A：先把联网链路跑通
如果当前目标是先证明：
- `ESP32 能保存 Wi‑Fi`
- `ESP32 能离开 AP 模式`
- `ESP32 能连上家里/办公室 Wi‑Fi`
- `ESP32 能访问你的 Mac 服务器`

那么下一步可以先在配网页中填写：
- `WiFi SSID`：你希望 ESP32 之后加入的那个路由器 Wi‑Fi 名称
- `密码`：对应 Wi‑Fi 密码
- `服务器(host:port)`：`10.142.16.119:8765`（仅当该 IP 对 ESP32 可达）
- `每日刷新时间`：先保持默认 `8`
- `时区`：保持 `+8`

然后点击：
- `保存并重启`

### 路线 B：先准备真实 BIN 文件，再让它联网拉图
如果当前目标是直接看到墨水屏刷新出项目内容，那么在保存配网前后，都还需要继续准备：
- 服务端静态 BIN 文件；
- 或先跑完整的 `analyze_photos.py -> render_daily_photo.py -> server.py` 链路。

## 模型需求补充

如果要使用项目原始链路“从照片自动分析并生成每日图文”，则需要一个兼容 OpenAI 接口的视觉模型服务。

仓库 README 明确写到：
- InkTime 使用 OpenAI 兼容接口；
- 作者提到 `qwen3-vl-30b` 已能取得不错效果。

当前 `config-example.py` 默认写的是：
- `MODEL_NAME = "qwen3-vl-32b-instruct"`
- `API_URL = "http://127.0.0.1:1234/v1/chat/completions"`

若想在本地先尽快跑通，建议优先考虑：
- `Qwen2.5-VL-7B-Instruct`：效果和资源占用更平衡，适合先验证流程；
- 如果机器资源更紧，可再降到更小的视觉模型；
- 如果追求更好的文案与审美判断，再考虑更大的 Qwen VL 模型。

参考：
- [InkTime README](https://github.com/placeholder/local-readme)（本条只作说明，实际项目 README 已在本地仓库）
- [Qwen2.5-VL-7B-Instruct](https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct)
- [Hugging Face Transformers: Qwen2.5-VL](https://huggingface.co/docs/transformers/model_doc/qwen2_5_vl)

## 当前推进位置

截至本轮，项目推进位置已更新为：
- `ESP32 成功烧录`
- `串口恢复并输出正常日志`
- `AP 热点 InkTime-090 成功启动`
- `配网页 http://192.168.4.1/ 已成功打开`
- `Mac 侧 Flask 服务已启动在 8765`
- `但 photo_*.bin 尚未生成，墨水屏最终内容刷新链路还差最后一段`

