# Arduino IDE 已选板，下一步参数设置记录

## 1. 当前推进状态

根据本轮最新截图，已经确认：

- `ink-display-7C-photo.ino` 已打开
- 板卡已成功选成：`ESP32S3 Dev Module`
- 当前已经从“未选板”推进到“板卡已确认”

这说明现在已经完成：

1. Arduino IDE 安装完成
2. Arduino IDE 打开完成
3. 项目文件打开完成
4. 开发板选择完成

当前接下来要做的是：

- 设置烧录参数
- 选择串口
- 再执行 Upload

---

## 2. 现在下一步怎么做

请按下面顺序继续设置。

### 第一步：打开 `Tools`

在 Arduino IDE 顶部菜单栏点击：

- `Tools`

### 第二步：设置这 4 项

依次设置为：

1. `Tools -> Flash Size -> 16MB`
2. `Tools -> PSRAM -> OPI PSRAM`
3. `Tools -> USB CDC On Boot -> Enabled`
4. `Tools -> Upload Speed -> 115200`

如果某一项菜单名字略有差异，以含义一致为准。

---

## 3. 然后继续设置串口

完成上面 4 项后，再去：

- `Tools -> Port`

选择：

- `/dev/cu.usbmodem5A470123231`

---

## 4. 上传前仍然建议先做的动作

在点击 `Upload` 前，仍建议：

- **先把墨水屏转接板到 ESP32 的所有杜邦线拔掉**
- 只保留裸板和 USB

原因是前面已经多次确认，当前烧录阻塞点还是：

```text
Failed to write to target RAM (result was 0107: Checksum error)
```

所以这一步是优先验证裸板烧录能否成功。

---

## 5. 本轮问答补充记录

### 用户问题

好了。

### 当前答复摘要

已根据截图确认：

- 板卡 `ESP32S3 Dev Module` 已选好
- 下一步应该进入 `Tools` 菜单设置：
  - `Flash Size = 16MB`
  - `PSRAM = OPI PSRAM`
  - `USB CDC On Boot = Enabled`
  - `Upload Speed = 115200`
- 然后在 `Tools -> Port` 中选 `/dev/cu.usbmodem5A470123231`
- 上传前仍建议先断开墨水屏，只用裸板烧录

---

## 6. 当前一句话结论

**现在请去 `Tools` 菜单，把 `Flash Size / PSRAM / USB CDC On Boot / Upload Speed` 四项设置好。**
